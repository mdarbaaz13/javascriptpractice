const heros=['shaktiman', 'hatim', 'powerranges']
const cartoon =['tomandjerry','noddy','ben10','oggy']
heros.push(cartoon)//this will not concatenate the two srrays instead it will add the cartoons whole array as a single element at the end of heros array....
console.log(heros);

